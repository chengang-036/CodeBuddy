# Firebase 配置指南

本指南将帮助您配置 Firebase，实现跨设备数据同步。

## 📋 步骤一：创建 Firebase 项目

1. **访问 Firebase Console**
   - 打开浏览器，访问：https://console.firebase.google.com/
   - 使用您的 Google 账号登录

2. **创建新项目**
   - 点击"添加项目"或"Create a project"
   - 项目名称：`CodeBuddy-Clothes`（或您喜欢的名字）
   - 是否启用 Google Analytics：可选（建议关闭，简化流程）
   - 点击"创建项目"

3. **等待项目创建完成**（约 30 秒）

## 📱 步骤二：注册 Web 应用

1. 在项目概览页面，点击 **Web 图标** `</>`
2. 填写应用昵称：`Clothes-App`
3. **不要**勾选"同时设置 Firebase Hosting"
4. 点击"注册应用"

5. **复制配置信息**
   
   您会看到类似这样的代码：
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSyXxXxXxXxXxXxXxXxXxXxXxXxXxXxX",
     authDomain: "codebuddy-clothes.firebaseapp.com",
     projectId: "codebuddy-clothes",
     storageBucket: "codebuddy-clothes.appspot.com",
     messagingSenderId: "123456789012",
     appId: "1:123456789012:web:abcdef1234567890"
   };
   ```

6. **复制这段配置**（稍后会用到）

## 🗄️ 步骤三：配置 Firestore Database

1. 在左侧菜单中，点击 **"Firestore Database"**
2. 点击 **"创建数据库"**
3. 选择模式：
   - **生产模式**（推荐）
   - 点击"下一步"
4. 选择位置：
   - 选择 **asia-east1 (台湾)** 或 **asia-east2 (香港)**
   - 点击"启用"
5. 等待数据库创建完成

6. **配置安全规则**
   - 点击顶部的 **"规则"** 标签
   - 将规则修改为：
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /clothes/{clothId} {
         allow read, write: if true;
       }
       match /outfits/{outfitId} {
         allow read, write: if true;
       }
     }
   }
   ```
   - 点击 **"发布"**
   
   ⚠️ 注意：这是最简单的规则，允许所有人读写。如果您想要更安全的配置，后续可以添加身份验证。

## 📦 步骤四：配置 Storage

1. 在左侧菜单中，点击 **"Storage"**
2. 点击 **"开始使用"**
3. 安全规则：
   - 选择 **"生产模式"**
   - 点击"下一步"
4. 选择位置：
   - 使用与 Firestore 相同的位置
   - 点击"完成"

5. **配置安全规则**
   - 点击顶部的 **"规则"** 标签
   - 将规则修改为：
   ```
   rules_version = '2';
   service firebase.storage {
     match /b/{bucket}/o {
       match /clothes/{allPaths=**} {
         allow read, write: if true;
       }
     }
   }
   ```
   - 点击 **"发布"**

## ⚙️ 步骤五：配置项目代码

1. **打开配置文件**
   - 编辑 `0003-Clothes/firebase-config.js` 文件

2. **替换配置信息**
   - 将您在"步骤二"中复制的配置信息粘贴进去
   - 替换 `YOUR_API_KEY`、`YOUR_PROJECT_ID` 等占位符

   修改后的文件应该类似：
   ```javascript
   const firebaseConfig = {
       apiKey: "AIzaSyXxXxXxXxXxXxXxXxXxXxXxXxXxXxX",
       authDomain: "codebuddy-clothes.firebaseapp.com",
       projectId: "codebuddy-clothes",
       storageBucket: "codebuddy-clothes.appspot.com",
       messagingSenderId: "123456789012",
       appId: "1:123456789012:web:abcdef1234567890"
   };
   
   if (typeof firebase !== 'undefined' && !firebase.apps.length) {
       firebase.initializeApp(firebaseConfig);
   }
   
   const db = firebase.firestore ? firebase.firestore() : null;
   const storage = firebase.storage ? firebase.storage() : null;
   
   console.log('Firebase 配置已加载');
   ```

3. **保存文件**

## 🚀 步骤六：测试

1. **打开云端版页面**
   - 在浏览器中打开 `0003-Clothes/upload-firebase.html`

2. **测试上传**
   - 上传一张照片
   - 填写描述和标签
   - 点击"保存上架"
   - 应该看到"正在上传到云端..."提示

3. **验证数据**
   - 返回 Firebase Console
   - 查看 Firestore Database，应该能看到新数据
   - 查看 Storage，应该能看到上传的图片

4. **跨设备测试**
   - 在手机上打开同一个网址
   - 应该能看到刚才上传的数据！

## ✅ 完成！

现在您的数据会自动同步到云端，所有设备都能访问了！

## 💡 常见问题

### 1. 上传失败，提示权限错误
- 检查 Firestore 和 Storage 的安全规则是否正确配置
- 确保规则已"发布"

### 2. 配置文件无效
- 确保所有引号和逗号都正确
- 检查是否有拼写错误

### 3. 想要更安全的配置
- 可以添加 Firebase Authentication
- 修改安全规则，要求用户登录后才能读写

### 4. 免费额度够用吗？
- Firestore: 每天 50,000 次读取，20,000 次写入
- Storage: 5GB 存储空间，1GB/天 下载流量
- 个人使用完全足够！

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看浏览器控制台的错误信息
2. 检查 Firebase Console 的配额使用情况
3. 告诉我具体的错误信息，我会帮您解决
