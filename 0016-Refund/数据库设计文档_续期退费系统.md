# 续期退费系统 - 数据库设计文档

**版本**: v2.0  
**日期**: 2026-07-01  
**数据库**: MySQL 8.0+ / PostgreSQL 14+

---

## 📋 目录

1. [数据库概览](#数据库概览)
2. [详细表结构设计](#详细表结构设计)
3. [索引设计](#索引设计)
4. [表关系说明](#表关系说明)
5. [建表 SQL](#建表-sql)
6. [开发注意事项](#开发注意事项)

---

## 数据库概览

### 核心业务表（5张）
| 表名 | 说明 | 主要用途 |
|------|------|----------|
| `policy` | 保单表 | 存储保单基本信息 |
| `payment` | 缴费记录表 | 存储历年缴费记录 |
| `natural_person` | 自然人信息表 | 存储投保人详细信息 |
| `scenario` | 场景规则表 | 退费前置校验规则 |
| `refund` | 退费记录表 | 退费申请主记录 |

### 权限管理表（5张）
| 表名 | 说明 | 主要用途 |
|------|------|----------|
| `user` | 用户表 | 系统用户 |
| `role` | 角色表 | 角色定义（J1-J8） |
| `permission` | 权限表 | 权限定义（Q1-Q9） |
| `user_role` | 用户角色关联表 | 用户-角色多对多 |
| `role_permission` | 角色权限关联表 | 角色-权限多对多 |

### 审批流表（3张）
| 表名 | 说明 | 主要用途 |
|------|------|----------|
| `approval_node` | 审批节点表 | 审批流程节点记录 |
| `approval_flow_config` | 审批流配置表 | 审批流默认配置 |
| `refund_log` | 操作日志表 | 操作记录 |

### 通知记录表（2张）
| 表名 | 说明 | 主要用途 |
|------|------|----------|
| `sms_record` | 短信记录表 | 短信通知记录 |
| `wechat_record` | 微信记录表 | 微信通知记录 |

### 组织机构表（2张）
| 表名 | 说明 | 主要用途 |
|------|------|----------|
| `branch` | 分公司表 | 分公司信息 |
| `branch_inst` | 所属机构表 | 中支机构信息 |

**总计**: 17张表

---

## 详细表结构设计

### 1. 保单表 (`policy`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `policy_no` | VARCHAR | 20 | PRIMARY KEY | 保单号（如 PB20200101001） |
| `holder_name` | VARCHAR | 100 | NOT NULL | 投保人姓名 |
| `holder_id` | VARCHAR | 18 | NOT NULL | 身份证号 |
| `holder_id_masked` | VARCHAR | 20 | | 身份证脱敏（如 310101********1234） |
| `phone` | VARCHAR | 11 | NOT NULL | 手机号 |
| `phone_masked` | VARCHAR | 15 | | 手机脱敏（如 138****5678） |
| `main_risk` | VARCHAR | 100 | | 主险名称 |
| `risk_code` | VARCHAR | 20 | | 险种代码 |
| `status` | VARCHAR | 20 | | 保单状态（有效/失效等） |
| `start_date` | DATE | | | 责任起期 |
| `end_date` | DATE | | | 责任止期 |
| `next_pay_date` | DATE | | | 下次缴费日期 |
| `pay_method` | VARCHAR | 20 | | 缴费方式（年缴/季缴等） |
| `branch` | VARCHAR | 50 | | 分公司 |
| `sub_branch` | VARCHAR | 50 | | 中支（所属机构，- 表示无） |
| `address` | VARCHAR | 200 | | 地址 |
| `account_name` | VARCHAR | 100 | | 账户名 |
| `bank_account` | VARCHAR | 30 | | 银行账号 |
| `bank_name` | VARCHAR | 50 | | 开户行 |
| `total_premium` | DECIMAL | 12,2 | | 总保费 |
| `sales_channel` | VARCHAR | 20 | | 销售渠道（个险/银保/团险/在线） |
| `sub_channel` | VARCHAR | 10 | | 子渠道（PAA/PSA/-） |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY (`policy_no`)
- INDEX `idx_holder_id` (`holder_id`)
- INDEX `idx_phone` (`phone`)
- INDEX `idx_branch` (`branch`, `sub_branch`)

---

### 2. 缴费记录表 (`payment`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `policy_no` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 保单号 |
| `period` | INT | | NOT NULL | 期次（第几期） |
| `amount` | DECIMAL | 12,2 | NOT NULL | 金额 |
| `should_pay_date` | DATE | | NOT NULL | 应缴日期 |
| `actual_pay_date` | DATE | | | 实缴日期 |
| `source` | VARCHAR | 20 | | 来源（授权账户/银行转账等） |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_policy_no` (`policy_no`)
- INDEX `idx_should_pay_date` (`should_pay_date`)

---

### 3. 自然人信息表 (`natural_person`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `policy_no` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 保单号 |
| `name` | VARCHAR | 100 | NOT NULL | 姓名 |
| `gender` | VARCHAR | 2 | | 性别（男/女） |
| `nationality` | VARCHAR | 20 | | 国籍 |
| `occupation` | VARCHAR | 50 | | 职业 |
| `address` | VARCHAR | 200 | | 地址 |
| `contact` | VARCHAR | 20 | | 联系方式 |
| `id_type` | VARCHAR | 20 | | 身份证件种类（居民身份证等） |
| `id_number` | VARCHAR | 18 | | 身份证件号码 |
| `valid_until` | DATE | | | 有效期限 |
| `follow_wechat` | TINYINT | 1 | DEFAULT 0 | 是否关注太平洋寿险官微（0否1是） |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_policy_no` (`policy_no`)

---

### 4. 场景规则表 (`scenario`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `policy_no` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 保单号 |
| `has_claim_after_due` | TINYINT | 1 | DEFAULT 0 | 应缴日后有理赔（0否1是） |
| `has_surrender_after_due` | TINYINT | 1 | DEFAULT 0 | 应缴日后有减保（0否1是） |
| `has_holder_change` | TINYINT | 1 | DEFAULT 0 | 已做投保人变更（0否1是） |
| `has_unpaid_loan` | TINYINT | 1 | DEFAULT 0 | 存在未结清贷款（0否1是） |
| `commission_payable` | TINYINT | 1 | DEFAULT 0 | 是否发放续期佣金（0否1是） |
| `advance_payment_refund` | TINYINT | 1 | DEFAULT 0 | 是否垫缴退费（0否1是） |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE INDEX `uk_policy_no` (`policy_no`)

---

### 5. 退费记录表 (`refund`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | VARCHAR | 20 | PRIMARY KEY | 退费ID（如 RF20250507001） |
| `policy_no` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 保单号 |
| `holder_name` | VARCHAR | 100 | NOT NULL | 投保人姓名 |
| `holder_id` | VARCHAR | 18 | NOT NULL | 身份证号 |
| `phone` | VARCHAR | 11 | NOT NULL | 手机号 |
| `amount` | DECIMAL | 12,2 | NOT NULL | 退费金额 |
| `status` | INT | | NOT NULL | 状态（1-11，见状态枚举） |
| `status_name` | VARCHAR | 50 | | 状态名称 |
| `accept_channel` | VARCHAR | 20 | | 受理渠道（95500电话/柜面等） |
| `accept_time` | DATETIME | | | 受理时间 |
| `acceptor` | VARCHAR | 50 | | 受理人 |
| `refund_periods` | VARCHAR | 100 | | 退费期次 |
| `refund_reason` | VARCHAR | 200 | | 退费原因 |
| `is_auto_approval` | TINYINT | 1 | DEFAULT 0 | 是否自动审批（0否1是） |
| `is_original_card` | TINYINT | 1 | DEFAULT 1 | 是否原卡原退（0否1是） |
| `material_deadline` | DATETIME | | | 材料补充截止时间 |
| `failure_reason` | VARCHAR | 200 | | 失败原因 |
| `sales_channel` | VARCHAR | 20 | | 销售渠道 |
| `sub_channel` | VARCHAR | 10 | | 子渠道 |
| `branch` | VARCHAR | 50 | | 分公司 |
| `sub_branch` | VARCHAR | 50 | | 所属机构 |
| `bank_account` | VARCHAR | 30 | | 银行卡号（非原卡原退时） |
| `bank_name` | VARCHAR | 50 | | 开户行 |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**状态枚举**:
- `1`: 受理中
- `2`: 受理撤销
- `3`: 审批未完成
- `4`: 审批第一个节点
- `5`: 材料待确认状态
- `6`: 材料已确认
- `7`: 审批通过
- `8`: 审批拒绝
- `9`: 退费处理中
- `10`: 退费失败
- `11`: 退费撤销

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_policy_no` (`policy_no`)
- INDEX `idx_holder_id` (`holder_id`)
- INDEX `idx_phone` (`phone`)
- INDEX `idx_status` (`status`)
- INDEX `idx_accept_time` (`accept_time`)
- INDEX `idx_branch` (`branch`, `sub_branch`)

---

### 6. 审批节点表 (`approval_node`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `refund_id` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 退费ID |
| `node_name` | VARCHAR | 100 | NOT NULL | 节点名称 |
| `node_order` | INT | | NOT NULL | 节点顺序 |
| `status` | VARCHAR | 20 | DEFAULT 'pending' | 状态（pending/completed/rejected） |
| `operator` | VARCHAR | 50 | | 操作人姓名 |
| `operator_id` | VARCHAR | 20 | | 操作人ID |
| `operate_time` | DATETIME | | | 操作时间 |
| `comment` | VARCHAR | 500 | | 审批意见 |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_refund_id` (`refund_id`)
- INDEX `idx_operator_id` (`operator_id`)

---

### 7. 操作日志表 (`refund_log`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `refund_id` | VARCHAR | 20 | NOT NULL, FOREIGN KEY | 退费ID |
| `time` | DATETIME | | NOT NULL | 操作时间 |
| `action` | VARCHAR | 50 | NOT NULL | 操作动作 |
| `operator` | VARCHAR | 50 | NOT NULL | 操作人 |
| `detail` | VARCHAR | 500 | | 操作详情 |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_refund_id` (`refund_id`)
- INDEX `idx_time` (`time`)

---

### 8. 用户表 (`user`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `user_id` | VARCHAR | 20 | PRIMARY KEY | 用户ID（如 U1） |
| `user_name` | VARCHAR | 50 | NOT NULL | 用户姓名 |
| `username` | VARCHAR | 50 | NOT NULL UNIQUE | 用户名（登录用） |
| `password_hash` | VARCHAR | 255 | NOT NULL | 密码哈希 |
| `role_id` | VARCHAR | 10 | | 主要角色ID（冗余字段，方便查询） |
| `branch` | VARCHAR | 50 | | 所属分公司 |
| `status` | TINYINT | 1 | DEFAULT 1 | 状态（1启用/0禁用） |
| `last_login` | DATETIME | | | 最后登录时间 |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY (`user_id`)
- UNIQUE INDEX `uk_username` (`username`)
- INDEX `idx_role_id` (`role_id`)

---

### 9. 角色表 (`role`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `role_id` | VARCHAR | 10 | PRIMARY KEY | 角色ID（如 J1） |
| `role_name` | VARCHAR | 50 | NOT NULL | 角色名称 |

**初始数据**:
- `J1`: 客户
- `J2`: 95500坐席
- `J3`: 柜员
- `J4`: 业务内勤
- `J5`: 审批人
- `J6`: 业务管理-分渠道
- `J7`: 业务管理-全渠道
- `J8`: Admin

---

### 10. 权限表 (`permission`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `perm_id` | VARCHAR | 10 | PRIMARY KEY | 权限ID（如 Q1） |
| `perm_name` | VARCHAR | 100 | NOT NULL | 权限名称 |

**初始数据**:
- `Q1`: 续期退费受理
- `Q2`: 续期退费查询和统计
- `Q3`: 续期退费审批
- `Q4`: 续期退费补充材料确认
- `Q5`: 续期退费失败件处理
- `Q6`: 续期退费进度查询（移动端）
- `Q7`: 续期退费补充材料（移动端）
- `Q8`: 续期退费运维
- `Q9`: 审批流管理

---

### 11. 用户角色关联表 (`user_role`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `user_id` | VARCHAR | 20 | NOT NULL | 用户ID |
| `role_id` | VARCHAR | 10 | NOT NULL | 角色ID |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE INDEX `uk_user_role` (`user_id`, `role_id`)
- INDEX `idx_role_id` (`role_id`)

---

### 12. 角色权限关联表 (`role_permission`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `role_id` | VARCHAR | 10 | NOT NULL | 角色ID |
| `perm_id` | VARCHAR | 10 | NOT NULL | 权限ID |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE INDEX `uk_role_perm` (`role_id`, `perm_id`)
- INDEX `idx_perm_id` (`perm_id`)

---

### 13. 短信记录表 (`sms_record`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `refund_id` | VARCHAR | 20 | NOT NULL | 退费ID |
| `phone` | VARCHAR | 11 | NOT NULL | 接收手机号 |
| `content` | VARCHAR | 500 | NOT NULL | 短信内容 |
| `category` | VARCHAR | 20 | | 类别（material/query/approval等） |
| `send_time` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 发送时间 |
| `status` | VARCHAR | 20 | DEFAULT 'sent' | 状态（sent/failed） |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_refund_id` (`refund_id`)
- INDEX `idx_phone` (`phone`)

---

### 14. 微信记录表 (`wechat_record`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `refund_id` | VARCHAR | 20 | NOT NULL | 退费ID |
| `wechat_id` | VARCHAR | 100 | | 微信OpenID |
| `content` | VARCHAR | 500 | NOT NULL | 微信内容 |
| `category` | VARCHAR | 20 | | 类别（material/query/approval等） |
| `send_time` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 发送时间 |
| `status` | VARCHAR | 20 | DEFAULT 'sent' | 状态（sent/failed） |

**索引**:
- PRIMARY KEY (`id`)
- INDEX `idx_refund_id` (`refund_id`)

---

### 15. 分公司表 (`branch`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | INT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `branch_name` | VARCHAR | 50 | NOT NULL UNIQUE | 分公司名称 |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

---

### 16. 所属机构表 (`branch_inst`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | BIGINT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `branch_name` | VARCHAR | 50 | NOT NULL | 分公司名称 |
| `inst_name` | VARCHAR | 50 | NOT NULL | 机构名称（- 表示无中支） |
| `created_at` | DATETIME | | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- UNIQUE INDEX `uk_branch_inst` (`branch_name`, `inst_name`)

---

### 17. 审批流配置表 (`approval_flow_config`)

| 字段名 | 数据类型 | 长度 | 约束 | 说明 |
|--------|----------|------|------|------|
| `id` | INT | | PRIMARY KEY AUTO_INCREMENT | 自增主键 |
| `config_key` | VARCHAR | 100 | NOT NULL UNIQUE | 配置键（如 hq_个险PAA） |
| `config_value` | JSON | | NOT NULL | 配置值（JSON格式） |
| `config_type` | VARCHAR | 20 | NOT NULL | 配置类型（hq/branch/inst） |
| `updated_at` | DATETIME | | ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**说明**:
- `config_key` 格式：
  - 总公司: `hq_个险PAA`, `hq_个险PSA`, `hq_在线`
  - 分公司: `branch_个险PAA_上海分公司`, `branch_银保_北京分公司`
  - 所属机构: `inst_个险PAA_上海分公司_-`, `inst_个险PAA_浙江分公司_杭州中支`
- `config_value` 存储 JSON 格式的审批节点配置

---

## 索引设计

### 核心业务索引
```sql
-- 退费记录表（最常用查询）
CREATE INDEX idx_refund_status_branch ON refund(status, branch);
CREATE INDEX idx_refund_accept_time ON refund(accept_time DESC);
CREATE INDEX idx_refund_holder_phone ON refund(holder_id, phone);

-- 审批节点表
CREATE INDEX idx_approval_refund_status ON approval_node(refund_id, status);

-- 操作日志表
CREATE INDEX idx_log_refund_time ON refund_log(refund_id, time DESC);
```

### 权限管理索引
```sql
-- 用户角色关联（频繁查询）
CREATE INDEX idx_user_role_user ON user_role(user_id);
CREATE INDEX idx_user_role_role ON user_role(role_id);

-- 角色权限关联（频繁查询）
CREATE INDEX idx_role_perm_role ON role_permission(role_id);
CREATE INDEX idx_role_perm_perm ON role_permission(perm_id);
```

---

## 表关系说明

### 一对多关系
1. **保单 (1) : (N) 缴费记录**
   - `payment.policy_no` → `policy.policy_no`
   
2. **保单 (1) : (1) 自然人信息**
   - `natural_person.policy_no` → `policy.policy_no`
   
3. **保单 (1) : (1) 场景规则**
   - `scenario.policy_no` → `policy.policy_no`
   
4. **退费记录 (1) : (N) 审批节点**
   - `approval_node.refund_id` → `refund.id`
   
5. **退费记录 (1) : (N) 操作日志**
   - `refund_log.refund_id` → `refund.id`
   
6. **退费记录 (1) : (N) 短信记录**
   - `sms_record.refund_id` → `refund.id`
   
7. **退费记录 (1) : (N) 微信记录**
   - `wechat_record.refund_id` → `refund.id`

### 多对多关系
1. **用户 (N) : (N) 角色**
   - 通过 `user_role` 关联表
   
2. **角色 (N) : (N) 权限**
   - 通过 `role_permission` 关联表

### 组织机构关系
1. **分公司 (1) : (N) 所属机构**
   - `branch_inst.branch_name` → `branch.branch_name`
   
2. **分公司 (1) : (N) 退费记录**
   - `refund.branch` → `branch.branch_name`

---

## 建表 SQL

### MySQL 版本

```sql
-- =====================================================
-- 续期退费系统数据库建表SQL (MySQL 8.0+)
-- 创建时间: 2026-07-01
-- =====================================================

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS = 0;

-- 1. 保单表
CREATE TABLE `policy` (
  `policy_no` varchar(20) NOT NULL COMMENT '保单号',
  `holder_name` varchar(100) NOT NULL COMMENT '投保人姓名',
  `holder_id` varchar(18) NOT NULL COMMENT '身份证号',
  `holder_id_masked` varchar(20) DEFAULT NULL COMMENT '身份证脱敏',
  `phone` varchar(11) NOT NULL COMMENT '手机号',
  `phone_masked` varchar(15) DEFAULT NULL COMMENT '手机脱敏',
  `main_risk` varchar(100) DEFAULT NULL COMMENT '主险名称',
  `risk_code` varchar(20) DEFAULT NULL COMMENT '险种代码',
  `status` varchar(20) DEFAULT NULL COMMENT '保单状态',
  `start_date` date DEFAULT NULL COMMENT '责任起期',
  `end_date` date DEFAULT NULL COMMENT '责任止期',
  `next_pay_date` date DEFAULT NULL COMMENT '下次缴费日期',
  `pay_method` varchar(20) DEFAULT NULL COMMENT '缴费方式',
  `branch` varchar(50) DEFAULT NULL COMMENT '分公司',
  `sub_branch` varchar(50) DEFAULT NULL COMMENT '中支',
  `address` varchar(200) DEFAULT NULL COMMENT '地址',
  `account_name` varchar(100) DEFAULT NULL COMMENT '账户名',
  `bank_account` varchar(30) DEFAULT NULL COMMENT '银行账号',
  `bank_name` varchar(50) DEFAULT NULL COMMENT '开户行',
  `total_premium` decimal(12,2) DEFAULT NULL COMMENT '总保费',
  `sales_channel` varchar(20) DEFAULT NULL COMMENT '销售渠道',
  `sub_channel` varchar(10) DEFAULT NULL COMMENT '子渠道',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`policy_no`),
  KEY `idx_holder_id` (`holder_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_branch` (`branch`,`sub_branch`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='保单表';

-- 2. 缴费记录表
CREATE TABLE `payment` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `policy_no` varchar(20) NOT NULL COMMENT '保单号',
  `period` int NOT NULL COMMENT '期次',
  `amount` decimal(12,2) NOT NULL COMMENT '金额',
  `should_pay_date` date NOT NULL COMMENT '应缴日期',
  `actual_pay_date` date DEFAULT NULL COMMENT '实缴日期',
  `source` varchar(20) DEFAULT NULL COMMENT '来源',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_policy_no` (`policy_no`),
  KEY `idx_should_pay_date` (`should_pay_date`),
  CONSTRAINT `fk_payment_policy` FOREIGN KEY (`policy_no`) REFERENCES `policy` (`policy_no`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='缴费记录表';

-- 3. 自然人信息表
CREATE TABLE `natural_person` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `policy_no` varchar(20) NOT NULL COMMENT '保单号',
  `name` varchar(100) NOT NULL COMMENT '姓名',
  `gender` varchar(2) DEFAULT NULL COMMENT '性别',
  `nationality` varchar(20) DEFAULT NULL COMMENT '国籍',
  `occupation` varchar(50) DEFAULT NULL COMMENT '职业',
  `address` varchar(200) DEFAULT NULL COMMENT '地址',
  `contact` varchar(20) DEFAULT NULL COMMENT '联系方式',
  `id_type` varchar(20) DEFAULT NULL COMMENT '身份证件种类',
  `id_number` varchar(18) DEFAULT NULL COMMENT '身份证件号码',
  `valid_until` date DEFAULT NULL COMMENT '有效期限',
  `follow_wechat` tinyint(1) DEFAULT '0' COMMENT '是否关注太平洋寿险官微',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_policy_no` (`policy_no`),
  CONSTRAINT `fk_natural_person_policy` FOREIGN KEY (`policy_no`) REFERENCES `policy` (`policy_no`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='自然人信息表';

-- 4. 场景规则表
CREATE TABLE `scenario` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `policy_no` varchar(20) NOT NULL COMMENT '保单号',
  `has_claim_after_due` tinyint(1) DEFAULT '0' COMMENT '应缴日后有理赔',
  `has_surrender_after_due` tinyint(1) DEFAULT '0' COMMENT '应缴日后有减保',
  `has_holder_change` tinyint(1) DEFAULT '0' COMMENT '已做投保人变更',
  `has_unpaid_loan` tinyint(1) DEFAULT '0' COMMENT '存在未结清贷款',
  `commission_payable` tinyint(1) DEFAULT '0' COMMENT '是否发放续期佣金',
  `advance_payment_refund` tinyint(1) DEFAULT '0' COMMENT '是否垫缴退费',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_policy_no` (`policy_no`),
  CONSTRAINT `fk_scenario_policy` FOREIGN KEY (`policy_no`) REFERENCES `policy` (`policy_no`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='场景规则表';

-- 5. 退费记录表
CREATE TABLE `refund` (
  `id` varchar(20) NOT NULL COMMENT '退费ID',
  `policy_no` varchar(20) NOT NULL COMMENT '保单号',
  `holder_name` varchar(100) NOT NULL COMMENT '投保人姓名',
  `holder_id` varchar(18) NOT NULL COMMENT '身份证号',
  `phone` varchar(11) NOT NULL COMMENT '手机号',
  `amount` decimal(12,2) NOT NULL COMMENT '退费金额',
  `status` int NOT NULL COMMENT '状态',
  `status_name` varchar(50) DEFAULT NULL COMMENT '状态名称',
  `accept_channel` varchar(20) DEFAULT NULL COMMENT '受理渠道',
  `accept_time` datetime DEFAULT NULL COMMENT '受理时间',
  `acceptor` varchar(50) DEFAULT NULL COMMENT '受理人',
  `refund_periods` varchar(100) DEFAULT NULL COMMENT '退费期次',
  `refund_reason` varchar(200) DEFAULT NULL COMMENT '退费原因',
  `is_auto_approval` tinyint(1) DEFAULT '0' COMMENT '是否自动审批',
  `is_original_card` tinyint(1) DEFAULT '1' COMMENT '是否原卡原退',
  `material_deadline` datetime DEFAULT NULL COMMENT '材料补充截止时间',
  `failure_reason` varchar(200) DEFAULT NULL COMMENT '失败原因',
  `sales_channel` varchar(20) DEFAULT NULL COMMENT '销售渠道',
  `sub_channel` varchar(10) DEFAULT NULL COMMENT '子渠道',
  `branch` varchar(50) DEFAULT NULL COMMENT '分公司',
  `sub_branch` varchar(50) DEFAULT NULL COMMENT '所属机构',
  `bank_account` varchar(30) DEFAULT NULL COMMENT '银行卡号',
  `bank_name` varchar(50) DEFAULT NULL COMMENT '开户行',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_policy_no` (`policy_no`),
  KEY `idx_holder_id` (`holder_id`),
  KEY `idx_phone` (`phone`),
  KEY `idx_status` (`status`),
  KEY `idx_accept_time` (`accept_time`),
  KEY `idx_branch` (`branch`,`sub_branch`),
  CONSTRAINT `fk_refund_policy` FOREIGN KEY (`policy_no`) REFERENCES `policy` (`policy_no`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='退费记录表';

-- 6. 审批节点表
CREATE TABLE `approval_node` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `refund_id` varchar(20) NOT NULL COMMENT '退费ID',
  `node_name` varchar(100) NOT NULL COMMENT '节点名称',
  `node_order` int NOT NULL COMMENT '节点顺序',
  `status` varchar(20) DEFAULT 'pending' COMMENT '状态',
  `operator` varchar(50) DEFAULT NULL COMMENT '操作人姓名',
  `operator_id` varchar(20) DEFAULT NULL COMMENT '操作人ID',
  `operate_time` datetime DEFAULT NULL COMMENT '操作时间',
  `comment` varchar(500) DEFAULT NULL COMMENT '审批意见',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_refund_id` (`refund_id`),
  KEY `idx_operator_id` (`operator_id`),
  CONSTRAINT `fk_approval_node_refund` FOREIGN KEY (`refund_id`) REFERENCES `refund` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审批节点表';

-- 7. 操作日志表
CREATE TABLE `refund_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `refund_id` varchar(20) NOT NULL COMMENT '退费ID',
  `time` datetime NOT NULL COMMENT '操作时间',
  `action` varchar(50) NOT NULL COMMENT '操作动作',
  `operator` varchar(50) NOT NULL COMMENT '操作人',
  `detail` varchar(500) DEFAULT NULL COMMENT '操作详情',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_refund_id` (`refund_id`),
  KEY `idx_time` (`time`),
  CONSTRAINT `fk_refund_log_refund` FOREIGN KEY (`refund_id`) REFERENCES `refund` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';

-- 8. 用户表
CREATE TABLE `user` (
  `user_id` varchar(20) NOT NULL COMMENT '用户ID',
  `user_name` varchar(50) NOT NULL COMMENT '用户姓名',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password_hash` varchar(255) NOT NULL COMMENT '密码哈希',
  `role_id` varchar(10) DEFAULT NULL COMMENT '主要角色ID',
  `branch` varchar(50) DEFAULT NULL COMMENT '所属分公司',
  `status` tinyint(1) DEFAULT '1' COMMENT '状态',
  `last_login` datetime DEFAULT NULL COMMENT '最后登录时间',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_role_id` (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 9. 角色表
CREATE TABLE `role` (
  `role_id` varchar(10) NOT NULL COMMENT '角色ID',
  `role_name` varchar(50) NOT NULL COMMENT '角色名称',
  PRIMARY KEY (`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 10. 权限表
CREATE TABLE `permission` (
  `perm_id` varchar(10) NOT NULL COMMENT '权限ID',
  `perm_name` varchar(100) NOT NULL COMMENT '权限名称',
  PRIMARY KEY (`perm_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限表';

-- 11. 用户角色关联表
CREATE TABLE `user_role` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `user_id` varchar(20) NOT NULL COMMENT '用户ID',
  `role_id` varchar(10) NOT NULL COMMENT '角色ID',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`,`role_id`),
  KEY `idx_role_id` (`role_id`),
  CONSTRAINT `fk_user_role_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`user_id`) ON DELETE CASCADE,
  CONSTRAINT `fk_user_role_role` FOREIGN KEY (`role_id`) REFERENCES `role` (`role_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';

-- 12. 角色权限关联表
CREATE TABLE `role_permission` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `role_id` varchar(10) NOT NULL COMMENT '角色ID',
  `perm_id` varchar(10) NOT NULL COMMENT '权限ID',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_perm` (`role_id`,`perm_id`),
  KEY `idx_perm_id` (`perm_id`),
  CONSTRAINT `fk_role_perm_role` FOREIGN KEY (`role_id`) REFERENCES `role` (`role_id`) ON DELETE CASCADE,
  CONSTRAINT `fk_role_perm_perm` FOREIGN KEY (`perm_id`) REFERENCES `permission` (`perm_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';

-- 13. 短信记录表
CREATE TABLE `sms_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `refund_id` varchar(20) NOT NULL COMMENT '退费ID',
  `phone` varchar(11) NOT NULL COMMENT '接收手机号',
  `content` varchar(500) NOT NULL COMMENT '短信内容',
  `category` varchar(20) DEFAULT NULL COMMENT '类别',
  `send_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '发送时间',
  `status` varchar(20) DEFAULT 'sent' COMMENT '状态',
  PRIMARY KEY (`id`),
  KEY `idx_refund_id` (`refund_id`),
  KEY `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='短信记录表';

-- 14. 微信记录表
CREATE TABLE `wechat_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `refund_id` varchar(20) NOT NULL COMMENT '退费ID',
  `wechat_id` varchar(100) DEFAULT NULL COMMENT '微信OpenID',
  `content` varchar(500) NOT NULL COMMENT '微信内容',
  `category` varchar(20) DEFAULT NULL COMMENT '类别',
  `send_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '发送时间',
  `status` varchar(20) DEFAULT 'sent' COMMENT '状态',
  PRIMARY KEY (`id`),
  KEY `idx_refund_id` (`refund_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='微信记录表';

-- 15. 分公司表
CREATE TABLE `branch` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `branch_name` varchar(50) NOT NULL COMMENT '分公司名称',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_branch_name` (`branch_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分公司表';

-- 16. 所属机构表
CREATE TABLE `branch_inst` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `branch_name` varchar(50) NOT NULL COMMENT '分公司名称',
  `inst_name` varchar(50) NOT NULL COMMENT '机构名称',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_branch_inst` (`branch_name`,`inst_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='所属机构表';

-- 17. 审批流配置表
CREATE TABLE `approval_flow_config` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `config_key` varchar(100) NOT NULL COMMENT '配置键',
  `config_value` json NOT NULL COMMENT '配置值',
  `config_type` varchar(20) NOT NULL COMMENT '配置类型',
  `updated_at` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='审批流配置表';

SET FOREIGN_KEY_CHECKS = 1;
```

---

## 开发注意事项

### 1. 数据安全
- **敏感字段加密**: `holder_id`（身份证号）、`phone`（手机号）、`bank_account`（银行账号）需加密存储
- **数据脱敏**: 查询时自动脱敏显示（如 `310101********1234`）
- **权限控制**: 严格按角色权限控制数据访问范围

### 2. 性能优化
- **分页查询**: 退费记录表数据量大，必须分页查询（推荐每页 20-50 条）
- **索引优化**: 常用查询条件必须建立索引（如 `status` + `branch` 组合索引）
- **读写分离**: 高并发场景下建议读写分离

### 3. 数据一致性
- **事务管理**: 退费状态变更、审批节点更新、操作日志记录必须在同一事务中
- **乐观锁**: 高频更新字段（如 `status`）建议使用乐观锁防止并发问题

### 4. 审批流配置
- **JSON 存储**: `approval_flow_config.config_value` 使用 JSON 类型，灵活存储多级审批节点
- **配置缓存**: 审批流配置变更频繁，建议使用 Redis 缓存

### 5. 日期时间处理
- **时区统一**: 所有日期时间字段使用 `DATETIME` 类型，应用时统一转换为用户时区
- **截止时间**: `material_deadline`（材料补充截止时间）需设置定时任务自动检查过期

### 6. 移动端适配
- **接口设计**: 移动端接口需单独设计，返回字段精简
- **链接有效期**: 材料补充链接/进度查询链接需设置有效期（24h/72h）

### 7. 日志与审计
- **操作日志**: 所有关键操作（受理、审批、撤销等）必须记录日志
- **数据变更审计**: 建议使用数据库触发器或应用层记录数据变更历史

### 8. 备份与恢复
- **每日备份**: 生产环境建议每日全量备份 + 增量备份
- **灾难恢复**: 制定 RTO/RPO 目标，定期演练恢复流程

---

## 附录：ER 图

ER 图已单独生成，文件位置：
```
/Users/tiger/WorkBuddy/2026-05-07-task-2/0016-Refund/ER图_续期退费系统.html
```

---

**文档版本**: v2.0  
**最后更新**: 2026-07-01  
**维护人**: 开发团队
